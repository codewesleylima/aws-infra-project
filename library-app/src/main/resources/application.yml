spring:
  datasource:
    url: jdbc:h2:mem:booksdb;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always
server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: health,info
